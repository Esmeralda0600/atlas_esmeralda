"""Entry points de consola del paquete atlas."""


def ingest() -> None:
    """Comando ``atlas-ingest`` — NetCDF -> Zarr. Se implementa en M1."""
    raise SystemExit("atlas-ingest: pendiente de implementación (hito M1).")
